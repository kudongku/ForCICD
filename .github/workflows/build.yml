name: Build Docker Image

on:
  push:
    branches:
      - main

env:
  AWS_REGION: ap-northeast-2
  ECR_REGISTRY: 905418308591.dkr.ecr.ap-northeast-2.amazonaws.com/test-ecr # 레포 url
  ECR_REPOSITORY: test-ecr # 레포 이름
  IMAGE_TAG: latest

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        run: |
          aws configure set aws_access_key_id ${{ secrets.AWS_ACCESS_KEY }}
          aws configure set aws_secret_access_key ${{ secrets.AWS_SECRET_KEY }}
          aws configure set region ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build docker image
        run: |
          docker build -t test-ecr .

      - name: push ECR
        run: |
          docker tag test-ecr:latest 905418308591.dkr.ecr.ap-northeast-2.amazonaws.com/test-ecr:latest
          docker push 905418308591.dkr.ecr.ap-northeast-2.amazonaws.com/test-ecr:latest
